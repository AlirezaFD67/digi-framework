---
title: Documentation System Prompts
description: مجموعه prompt های آماده برای کار با سیستم داکیومنت‌اسیون
---

# 📚 Documentation System Prompts

این مجموعه شامل prompt های آماده برای کار با سیستم داکیومنت‌اسیون کامپوننت‌هاست.

## 🚀 اضافه کردن کامپوننت جدید

برای اضافه کردن کامپوننت جدید به سیستم داکیومنت‌اسیون، از prompt زیر استفاده کن:

[📁 اضافه کردن کامپوننت جدید](./add-component/)

## 🔄 آپدیت کامپوننت موجود

برای آپدیت کردن کامپوننت موجود در سیستم داکیومنت‌اسیون:

[📁 آپدیت کامپوننت موجود](./update-component/)

## 🔧 رفع خطاهای سیستم

برای رفع خطاهای مختلف در سیستم داکیومنت‌اسیون:

[📁 رفع خطاهای سیستم](./fix-errors/)

## 🏗️ سیستم Build و Registry

برای کار با سیستم Build و Registry:

[📁 سیستم Build و Registry](./build-system/)

## 📚 افزودن داکیومنت Framework

برای افزودن داکیومنت endpoint جدید به Framework Package:

[📁 افزودن داکیومنت Framework](./add-framework-docs/)

## 📋 دستورالعمل کلی

```
من می‌خوام یک کامپوننت جدید به سیستم داکیومنت‌اسیون اضافه کنم. لطفا مراحل زیر رو به ترتیب انجام بده:

1. **تحلیل کامپوننت**: کامپوننت رو بررسی کن و مشخصاتش رو تعیین کن
2. **ایجاد فایل‌های مورد نیاز**: فایل‌های registry و demo رو بساز
3. **آپدیت registry**: کامپوننت رو به registry اضافه کن
4. **ایجاد داکیومنت**: فایل MDX داکیومنت‌اسیون رو بساز
5. **Build registry**: اسکریپت build رو اجرا کن
6. **تست**: مطمئن شو همه چیز درست کار می‌کنه
```

## 📁 ساختار فایل‌ها

```
src/registry/
├── custom-ui/
│   ├── [component-name]/
│   │   ├── [component-name].tsx      # فایل demo اصلی
│   │   └── [component-name]-variants.tsx  # فایل demo variants
│   ├── index.ts                      # تنظیمات registry
│   └── __index__.ts                  # Auto-generated index

content/docs/custom-ui/
└── [component-name].mdx              # داکیومنت کامپوننت
```

## 🔧 مراحل پیاده‌سازی

### مرحله 1: ایجاد فولدر و فایل‌های Demo

```bash
# ایجاد فولدر کامپوننت
mkdir -p src/registry/custom-ui/[component-name]
```

```typescript
// src/registry/custom-ui/[component-name]/[component-name].tsx
"use client"

import { [ComponentName] } from "@workspace/custom-ui"

export function [ComponentName]Demo() {
  return (
    <div className="space-y-4">
      <div className="space-y-2">
        <h3 className="text-lg font-semibold">Basic Usage</h3>
        <div className="p-4 border rounded-lg">
          <[ComponentName]>
            Real content here
          </[ComponentName]>
        </div>
      </div>
    </div>
  )
}
```

### مرحله 2: آپدیت Registry Config

```typescript
// src/registry/index.ts
export const registry = {
  items: [
    // کامپوننت‌های موجود...
    {
      name: "[component-name]",
      description: "توضیح کوتاه کامپوننت",
      type: "registry:component",
      registryDependencies: undefined,
      files: [
        {
          path: "custom-ui/[component-name].tsx",
          type: "registry:component",
          target: "components/[component-name].tsx"
        }
      ],
      categories: ["components"],
      meta: {
        title: "[ComponentName]",
        description: "توضیح کامل کامپوننت"
      }
    }
  ]
}
```

### مرحله 3: ایجاد داکیومنت MDX

```markdown
---
title: [ComponentName]
description: توضیح کامپوننت
component: true
links:
  doc: https://ui.shadcn.com/docs/components/[component-name]
  api: https://ui.shadcn.com/docs/components/[component-name]#api-reference
---

<ComponentPreview
  name="[component-name]"
  description="توضیح کوتاه کامپوننت"
  align="center"
/>

## Usage

The component is already available in your project. You can use it by importing:

```tsx
import { [ComponentName] } from "@workspace/custom-ui"
```

<ComponentSource
  name="[component-name]"
  title="components/[component-name].tsx"
/>

## API Reference

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| ... | ... | ... | ... |
```

### مرحله 4: اجرای Build Script

```bash
npx tsx scripts/build-registry.ts
```

### مرحله 5: آپدیت Navigation

```json
// content/docs/custom-ui/meta.json
{
  "pages": {
    "custom-button": "Custom Button",
    "[component-name]": "[Component Name]"
  }
}
```

## 📝 قوانین مهم

1. **نام‌گذاری**: از kebab-case برای فایل‌ها و camelCase برای کامپوننت‌ها استفاده کن
2. **Import Path**: همیشه از `@workspace/custom-ui` import کن
3. **فولدرسازی**: هر کامپوننت باید فولدر جداگانه داشته باشه:
   - `src/registry/custom-ui/[component-name]/`
   - فایل اصلی: `[component-name].tsx`
4. **Demo Structure**: فقط یک مثال ساده و کاربردی نشون بده:
   - Basic Usage (استفاده پایه)
5. **Documentation**: داکیومنت کامل با مثال‌ها، API reference و usage بنویس
6. **Registry**: کامپوننت رو به registry اضافه کن
7. **Build**: حتماً اسکریپت build رو اجرا کن
8. **Installation**: از بخش Installation استفاده نکن، فقط Usage با import statement بذار
9. **Demo Content**: هر مثال باید محتوای واقعی و قابل نمایش داشته باشه

## 🎯 مثال کامل

```
کامپوننت: CustomInput
- فایل: custom-input.tsx
- Demo: CustomInputDemo (فقط یک مثال ساده)
- Registry: custom-input
- Documentation: custom-input.mdx
```

## ✅ چک‌لیست نهایی

- [ ] فایل demo ساده ساخته شده
- [ ] Registry config آپدیت شده
- [ ] داکیومنت MDX ایجاد شده
- [ ] Build script اجرا شده
- [ ] Navigation آپدیت شده
- [ ] کامپوننت در مرورگر تست شده

## 🎨 راهنمای ساخت Demo

### ساختار Demo ساده:

```typescript
export function [ComponentName]Demo() {
  return (
    <div className="space-y-4">
      {/* Basic Usage */}
      <div className="space-y-2">
        <h3 className="text-lg font-semibold">Basic Usage</h3>
        <div className="p-4 border rounded-lg">
          <[ComponentName]>
            Real content here
          </[ComponentName]>
        </div>
      </div>
    </div>
  )
}
```

## 🚨 نکات مهم

- همیشه از `"use client"` در فایل‌های demo استفاده کن
- Import path ها رو درست بنویس
- داکیومنت رو کامل و مفصل بنویس
- بعد از هر تغییر، build script رو اجرا کن
- کامپوننت رو در مرورگر تست کن
- **هرگز از بخش Installation استفاده نکن** - فقط Usage با import statement
- **Demo ها باید محتوای واقعی داشته باشند** - نه فقط placeholder
- **همیشه فولدرسازی کن** - هر کامپوننت فولدر جداگانه داشته باشه
- **فقط یک مثال ساده و کاربردی بساز** - نیازی به variants نیست

---

**حالا کامپوننت جدید رو بهم بده تا برات پیاده‌سازی کنم! 🚀**
